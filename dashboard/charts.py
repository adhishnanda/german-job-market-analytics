"""Reusable chart helpers for the Streamlit dashboard."""
from __future__ import annotations

import pandas as pd


def skill_trend_chart(df: pd.DataFrame) -> None:
    """Render a skill-demand trend line chart."""
    raise NotImplementedError


def salary_distribution_chart(df: pd.DataFrame) -> None:
    """Render a salary distribution chart."""
    raise NotImplementedError
